import { PushSubscriptionsController } from "./push-subscriptions.controller";

describe("PushSubscriptionsController", () => {
  it("exports association endpoints controller", () => {
    expect(PushSubscriptionsController).toBeDefined();
  });
});
