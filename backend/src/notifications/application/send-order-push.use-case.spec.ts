import { SendOrderPushUseCase } from "./send-order-push.use-case";

describe("SendOrderPushUseCase", () => {
  it("exports the current push use case", () => {
    expect(SendOrderPushUseCase).toBeDefined();
  });
});
