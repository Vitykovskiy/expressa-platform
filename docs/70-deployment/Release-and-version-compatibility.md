# Release and version compatibility

Развёртываемая единица — набор трёх immutable image digest. Development создаёт набор из SHA коммита. Staging использует [deploy/staging.env](../../deploy/staging.env) из тега `staging-v*`.

Production release не определён.
