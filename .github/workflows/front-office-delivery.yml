name: Front-office delivery

on:
  push:
    tags: ['front-office-v*']

permissions:
  contents: read
  packages: write

jobs:
  deliver:
    uses: ./.github/workflows/delivery-component.yml
    with:
      component: front-office
      context: front-office
      image: expressa-front-office
